/**
 * This <code>Command</code> interface is used as a base command.
 *
 * @author Cory Steudeman
 * @version 0.1
 * @date May 2, 2018
 */
public interface Command {
    /**
     * This <code>run</code> method is used to run the command.
     */
    void run();
}